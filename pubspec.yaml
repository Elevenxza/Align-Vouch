name: align_vouch_new
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # LIBRARY AUTHENTICATION
  flutter_web_auth_2: ^3.0.0
  http: ^1.2.0

  # LIBRARY SUPABASE (Ini yang lu butuhin!)
  supabase_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true